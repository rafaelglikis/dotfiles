Always run the code through YAGNI, SOLID, DRY, KISS principles.
The `gh` command has two authenticated accounts. If a repository cannot be found with the active account, run `gh auth switch` and retry with the other account.
